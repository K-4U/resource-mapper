{
	"name": "resource-mapper-monorepo",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "npm run dev -w @mapper/core",
		"build": "npm run build -w @mapper/core",
		"postinstall": "npm run download-icons -w @mapper/core"
	}
}
