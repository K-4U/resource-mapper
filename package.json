{
	"name": "resource-mapper-monorepo",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"license": "MIT",
	"engines": {
		"node": ">=22.11.0"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"postinstall": "npm run build -w @mapper/engine && npm run download-icons -w @mapper/engine"
	}
}
