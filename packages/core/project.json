{
  "name": "core",
  "projectType": "application",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/core"
      }
    },
    "publish": {
      "command": "npm publish --access public",
      "dependsOn": ["build"]
    }
  }
}