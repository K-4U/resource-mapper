friendlyName: "Apache Web Server"
description: |
  Primary web server handling HTTP/HTTPS requests for the web application.
  
  Serves as the entry point for all web traffic, providing load balancing,
  SSL termination, and routing to backend services. Configured with auto-scaling
  to handle varying traffic loads.
serviceType: ALB
outgoingConnections:
  - port: 3306
    targetIdentifier: "web/database"
    description: "MySQL database connection"
